window.addEventListener('resize', function() {    let button = document.querySelector('.header__button--booking');    const header = document.querySelector('.header__div--fame-logos');    if(window.innerWidth < '576'){        button.style.top = `${window.innerHeight}px`;        header.style.top = `${window.innerHeight - (window.innerHeight * 0.1 + header.offsetHeight)}px`;    } else {        button.removeAttribute('style');        header.removeAttribute('style');    }});window.addEventListener('load', function() {    let button = document.querySelector('.header__button--booking');    const header = document.querySelector('.header__div--fame-logos');    if(window.innerWidth < '576'){        button.style.top = `${window.innerHeight}px`;        header.style.top = `${window.innerHeight - (window.innerHeight * 0.1 + header.offsetHeight)}px`;    } else {        button.removeAttribute('style');        header.removeAttribute('style');    }})